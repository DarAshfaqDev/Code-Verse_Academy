# CodeVerse API

Express API scaffold for authentication, course data and dashboard metrics.

## Routes

- `GET /api/health`
- `GET /api/courses`
- `GET /api/dashboard`
- `POST /api/auth/login`

MongoDB or PostgreSQL can be connected from `server/config` when persistent storage is ready.
